describe('Directive: znActions', znActions);

function znActions() {
	var el;

	beforeEach(module('ZN'));

	beforeEach(inject(function($compile, $rootScope) {
		el = angular.element();
	}));
}